import inertia from '@inertiajs/vite'
import vue from '@vitejs/plugin-vue'
import { defineConfig } from 'vite'

export default defineConfig(({ isSsrBuild }) => ({
  plugins: [vue(), inertia({ ssr: [[% if ssr.enabled %]]{ entry: 'src/main.ts', host: '[[= ssr.host]]', port: [[= ssr.port]], cluster: false }[[% else %]]false[[% endif %]] })],
  build: { outDir: isSsrBuild ? 'dist/ssr' : 'dist', emptyOutDir: true, manifest: !isSsrBuild, rolldownOptions: isSsrBuild ? {} : { input: 'src/main.ts' } },
}))
