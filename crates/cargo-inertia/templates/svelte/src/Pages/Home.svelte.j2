<script lang="ts">
  import { Deferred } from '@inertiajs/svelte'
  // After adding a typed Rust page and running types:sync:
  // import type { PagePropsFor } from '$lib/types/inertia'

  type Stats = { projects: number; tasks: number }

  let { greeting = 'Hello', stats }: { greeting?: string; stats?: Stats } = $props()
</script>

<main>
  <h1>{greeting} from inertia-axum</h1>
  <Deferred data="stats">
    {#snippet fallback()}
      <p>Loading stats…</p>
    {/snippet}
    {#if stats}
      <p>{stats.projects} projects · {stats.tasks} tasks</p>
    {/if}
  </Deferred>
</main>
