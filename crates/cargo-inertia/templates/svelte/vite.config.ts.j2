import { svelte } from '@sveltejs/vite-plugin-svelte'
import { defineConfig } from 'vite'

export default defineConfig({ plugins: [svelte()], build: { manifest: true, rolldownOptions: { input: 'src/main.ts' } } })
