[package]
name = "axum-minimal"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
anyhow = "1"
axum = { workspace = true, features = ["http1", "tokio"] }
inertia-axum = { path = "../.." }
tokio = { workspace = true, features = ["macros", "net", "rt-multi-thread"] }
